<%= form_for [@list, @list.items.build] do |f| %>    
  <%= f.text_field :name, class: 'form-control', placeholder: "Enter to-do item name" %>
  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success btn-md savebtn' %>
  </div>   
<% end %>